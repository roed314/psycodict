# Documentation build dependencies.  Used by the docs job in
# .github/workflows/ci.yml and by Read the Docs (.readthedocs.yaml);
# the package itself (with a psycopg extra) must also be installed so
# that autodoc can import it.
# Pinned exactly: the docs build runs with -W (and fail_on_warning on Read
# the Docs), where a surprise major upgrade of any of these can turn a
# previously green build red with no change to the repository.  Bump the
# pins deliberately, together with a passing local `sphinx -W` build.
sphinx==9.1.0
myst-parser==5.1.0
furo==2025.12.19
